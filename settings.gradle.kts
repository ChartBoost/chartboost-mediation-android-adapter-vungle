pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

rootProject.name = "VungleAdapter"
include(":VungleAdapter")
include(":android-helium-sdk")
include(":Helium")
